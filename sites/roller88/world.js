class World {
    constructor(background, foreground, collision, map) {
        this.background = background;
        this.foreground = foreground;
        this.collision = collision;
        this.map = map;
    }
}